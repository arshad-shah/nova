# T-105: Window shell: frameless/overlay title bar, drag regions, window controls + maximize events

- status: open
- phase: 1
- depends_on: [T-101, T-104]
- touches: [src-tauri/src/main.rs, src-tauri/tauri.conf.json, src-tauri/capabilities/**, src-tauri/crates/verql-ipc/src/window.rs, src/renderer/src/components/shell/TitleBar.tsx, src/renderer/src/components/shell/WindowControls.tsx, src/renderer/src/components/shell/MenuBar.tsx, src/renderer/src/styles/globals.css]
- reads: [specs/tauri-migration/subsystems/window-shell-menus.md, specs/tauri-migration/spikes/T-009-webview-smoke/report.md, src/main/index.ts, src/main/ipc/window.ts, src/renderer/src/components/shell/TitleBar.tsx, src/renderer/src/components/shell/WindowControls.tsx]
- risk: high
- estimate: M
- verify.scope: rust, renderer

## Goal
The Tauri window reproduces v1's shell (`src/main/index.ts`): renderer-owned
title bar on every platform — macOS `hiddenInset`-equivalent overlay with
repositioned traffic lights, Windows/Linux frameless with app-drawn controls
— plus the window-control channels and the `window:maximize-changed`
broadcast, so `TitleBar`/`WindowControls` behave identically under Tauri.

## Acceptance criteria
- [ ] 1. Per-platform decoration parity: macOS keeps native traffic lights
      overlaid on the renderer bar via the **first-class
      `trafficLightPosition` window config** (Tauri ≥2.4; requires
      `titleBarStyle: "Overlay"` + `decorations: true` —
      versions-baseline); Windows and Linux run `decorations: false` with
      the renderer's own min/max/close controls (Tauri has no Windows WCO
      equivalent anyway — issue #12930 — and v1 deliberately avoided WCO;
      see the comment block in `src/main/index.ts`).
- [ ] 2. Drag regions work: v1's `.drag-region` / `.no-drag` CSS
      (`-webkit-app-region`) does nothing under Tauri — apply
      `data-tauri-drag-region` per the T-009 findings such that: the title
      bar drags the window; `WindowControls`, `MenuBar` buttons, and tab
      strip remain clickable (the attribute applies **only to the element
      it is set on**, never children — verify the bar's *empty* areas drag,
      interactive children don't); **double-click-maximize is manual** on
      Win/Linux (Tauri does not provide it). The required `core:window:*`
      capability entries exist (`allow-start-dragging`, minimize/maximize/
      close/fullscreen, etc.). Keep the Electron CSS intact for the v1 path
      (both shells run until cutover).
- [ ] 3. Dispatch arms implemented in `verql-ipc` window.rs (stubs →
      real), matching `src/main/ipc/window.ts` semantics exactly:
      `window:minimize` (void), `window:toggle-maximize` (**returns the
      new isMaximized bool**), `window:close`, `window:is-maximized`
      (bool), `window:set-titlebar-height` (macOS-only: recenter traffic
      lights at x=15, y=(height-12)/2 rounded, ignore non-finite/≤0; no-op
      elsewhere). All act on the **calling** window, not "focused".
- [ ] 4. `window:maximize-changed` (bool payload) is emitted via the T-102
      helper on every real state change — including OS-initiated ones
      (snap, double-click, unmaximize), mirroring v1's
      `maximize`/`unmaximize` listeners; the renderer's max/restore icon
      stays in sync without polling.
- [ ] 5. Burndown: the five channels above flip to `migrated`;
      `window:menu:list` / `window:menu:popup` stay `wontport`.
- [ ] 6. Renderer diffs are confined to the four listed files, and the
      Electron path still passes: `pnpm dev` shows the v1 shell unchanged;
      story tests for shell components stay green.

## Verify
```bash
cargo fmt --manifest-path src-tauri/Cargo.toml --check
cargo clippy --manifest-path src-tauri/Cargo.toml --workspace --all-targets -- -D warnings
cargo test --manifest-path src-tauri/Cargo.toml -p verql-ipc
pnpm exec tsc -b --noEmit
pnpm exec vitest run --project unit
grep -c "migrated" specs/tauri-migration/tasks/channel-burndown.md
```

## Notes
- Traffic-light repositioning is first-class config since Tauri 2.4
  (`trafficLightPosition`, commit 30f5a15) — **no private-API/objc2
  workaround**. The *behavior* contract is v1's: renderer reports real bar
  height (density-dependent) over `window:set-titlebar-height`, buttons
  recenter (re-apply the position from the Rust handler). If macOS
  hardware is unavailable, implement + unit-test the math, mark the manual
  check as debt for T-199's smoke matrix.
- `TitleBar.tsx` uses `env(titlebar-area-*)` (Windows WCO vars) — these
  **do not exist under Tauri** (no WCO, issue #12930): the layout must not
  depend on them and must be correct with the CSS fallback values on every
  platform; verify that, then leave the CSS for the Electron path.
- Capability entries live in `src-tauri/capabilities/` (shared with T-108
  — additive entries only; coordinate).
- Manual behavior verification (drag, snap, double-click) is scripted in
  T-199's checklist — this task's Log should record a first pass on the
  dev platform.
- `touches` intersects T-106 on `src-tauri/src/main.rs` — orchestrator
  serializes these two.

## Log
(empty)
