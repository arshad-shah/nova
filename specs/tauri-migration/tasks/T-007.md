# T-007: Spike: Snowflake SQL REST API (auth, query, introspection)

- status: open
- phase: 0
- depends_on: [T-001]
- touches: [specs/tauri-migration/spikes/T-007-snowflake-rest/**, specs/tauri-migration/decisions/ADR-0004-database-crates.md]
- reads: [specs/tauri-migration/subsystems/drivers.md, specs/tauri-migration/decisions/ADR-0004-database-crates.md, src/main/plugins/bundled/snowflake/index.ts, src/main/plugins/bundled/snowflake/snowflake-adapter.ts]
- risk: high
- estimate: M
- verify.scope: none (spike; report + ADR checks below)

## Goal
A written report proving (or refuting, before Phase 3 commits) that the
Snowflake SQL REST API v2 covers the v1 feature surface the `snowflake`
bundled plugin actually uses, and an ADR-0004 amendment recording what the
spike learned. T-308 implements from this report.

## Acceptance criteria
- [ ] 1. **Auth modes.** The report confirms hands-on (live account, or
      fully-cited docs where none is available) the baseline finding
      (versions-baseline.md §Snowflake): SQL API v2 accepts **only
      OAuth / key-pair JWT / PAT / WIF — no basic password auth at all** —
      with a demonstrated request for key-pair JWT (v1: `privateKeyPath` +
      optional `passphrase` → `SNOWFLAKE_JWT`) and PAT. It maps each v1
      connect() branch to its v2 fate: password → unsupported by REST (and
      dying anyway — MFA enforcement M2 now, M3 Aug–Oct 2026);
      `externalbrowser` (v1's **default**, SSO) — if REST cannot do
      browser-SSO, name and size the gap and escalate to the human owner
      via the ADR amendment; key-pair JWT → supported. Nothing is silently
      dropped.
- [ ] 1b. **Deliverables from criterion 1**: (i) the recommended v2
      connection-field set (account, key-pair fields
      privateKeyPath/passphrase, PAT token) and (ii) what happens to
      existing password-based v1 profiles at migration — the sanctioned,
      product-visible deviation per ADR-0004 §snowflake, spelled out as UX
      (clear error / re-auth prompt), not left implicit.
- [ ] 2. **Query lifecycle.** How to submit a statement
      (`POST /api/v2/statements`), when responses are synchronous vs
      202+`statementHandle`, how to poll for completion, and how multi-
      partition results are fetched and stitched — with the shape of
      `resultSetMetaData`/`rowType` documented against what v1's adapter
      returns in `QueryResult` (column names/types, row value encoding —
      REST returns strings + type metadata; note the conversion burden).
- [ ] 3. **Introspection.** How each v1 adapter call maps: `SHOW WAREHOUSES`
      / `SHOW ROLES` (used by the toolbar resolvers, incl. the
      quoted-lowercase column-name quirk noted in `snowflake-adapter.ts`),
      `getTables`/`getColumns` via SHOW or INFORMATION_SCHEMA, and
      database/schema switching + `switchWarehouse`/`switchRole` (per-
      request `warehouse`/`role` fields vs session `USE` statements —
      which one preserves v1 semantics).
- [ ] 4. **Cancellation & timeout.** How to abort a running statement
      (`POST /api/v2/statements/{handle}/cancel`) and how a request-level
      timeout maps — enabling the sanctioned fix for the v1 defect
      "queryTimeout not enforced" (T-308 asserts corrected behavior).
- [ ] 5. **Rate limits & polling.** Documented request/rate limits and what
      they imply for the poll interval/backoff design; a recommended
      polling policy is stated.
- [ ] 6. **Fallback evaluated**: `snowflake-connector-rs` 1.0 (active;
      key-pair/PAT/OAuth-capable per versions-baseline.md) is assessed as
      the fallback if owning the REST client stalls — a one-page verdict on
      feature coverage vs the v1 surface, MSRV 1.88 impact, and when to
      pull the ripcord.
- [ ] 7. ADR-0004's Snowflake row gets an appended amendment: confirmed /
      amended, the auth-mode matrix + connection-field recommendation, and
      any parity gaps accepted by the human owner.

## Verify
```bash
test -f specs/tauri-migration/spikes/T-007-snowflake-rest/report.md
for topic in externalbrowser statementHandle cancel "rate limit" SHOW \
             PAT "key-pair" MFA snowflake-connector-rs; do
  grep -qi "$topic" specs/tauri-migration/spikes/T-007-snowflake-rest/report.md \
    || { echo "unanswered: $topic"; exit 1; }
done
grep -qi "amendment" specs/tauri-migration/decisions/ADR-0004-database-crates.md
```

## Notes
- No seeded docker fixture exists for Snowflake; a live trial account is the
  realistic path — if none is obtainable, say so per criterion 1 and mark
  the affected answers documentary, not tested.
- v1 auth selection logic (`snowflake-adapter.ts` `connect()`): privateKey
  path present → `SNOWFLAKE_JWT`; else authenticator set and no password →
  passthrough (externalbrowser); else username/password. The report's
  matrix covers exactly these three branches — knowing password cannot map
  (criterion 1) is the point; the matrix records the disposition of each.
- PoC code (a minimal reqwest client) lives under the spike dir only.

## Log
(empty)
