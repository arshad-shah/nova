# Gate reports

Phase-gate results land here as `phase-<n>-<date>.md`: the command
transcript summary, parity-suite results, performance measurements, the
platforms actually exercised, and the verifier's verdict. Written by phase
gate verifier agents, committed by the orchestrator. See
`../../orchestration/verification.md`.
