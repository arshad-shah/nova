# Channel burndown

Regenerated by the orchestrator at every merge: for each of the 143 invoke
channels + 15 events in `shared/ipc.ts`, the Rust dispatch status.

Statuses: `not_migrated` (stub returning `NOT_MIGRATED`), `migrated`
(implemented + parity fixture passing), `wontport` (dead in v1 — deleted at
cutover; currently: `window:menu:list`, `window:menu:popup`).

> Not yet generated — first produced by T-102, which registers all channels
> as `not_migrated` stubs and emits the initial table here.
