// Shim: ActivityList moved to `./activity/ActivityList`. Re-exported here so the
// existing story import path (`./ActivityList`) keeps working through the split.
export { ActivityList, type ActivityListProps } from './activity'
